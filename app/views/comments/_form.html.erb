<h1><%= @comment.id ? 'Edit Comment' : 'Add Comment' %></h1>
<br />
<%= form_for [@movie, @comment] do |f| %>
  <%= f.label :body %>
  <%= f.text_area :body, required: true %>
  <br />
  <%= f.submit %>
<% end %>